Name: Bipin Shrestha
Programming language = Python 3.11.0
Code structure = All the code is in expense_8_py. The code run by reading the system argument from the console and perform by method by calling respective functions.
To run the code:
    python3 expense_8_puzzle.py start.txt goal.txt method dumpfile 

method can be BFS, UCS, astar, DFS, DLS
if the method is not provided the default is astar.  

dumpfile can be true or false
It the dumpfile is not provided in the console the default is false.


when running with a* in the terminal the terminal shows zsh: no matches found: a* in mac os. So to run the A* or a* use astar 



